<?php
/**
 * Created by IntelliJ IDEA.
 * User: Sadako
 * Date: 2016/4/15
 * Time: 17:40
 */

/**
 * Implements hook_menu().
 */
function about_menu() {
    $items['about'] = array(
        'title' => '产业园基地',
        'page callback' => 'about_introduction_page_callback',
        'access callback' => TRUE,
        'type' => MENU_SUGGESTED_ITEM,
        'menu_name' => 'main-menu',
        'weight'=>-2,
    );
    $items['about/introduction'] = array(
        'title' =>'企业介绍',
        'access callback' => TRUE,
        'type' => MENU_DEFAULT_LOCAL_TASK,
    );
    $items['about/parkstyle'] = array(
        'title' =>'园区风貌',
        'page callback' => 'about_parkstyle_page_callback',
        'access callback' => TRUE,
        'type' => MENU_LOCAL_TASK,
    );
    $items['about/parkstyle'] = array(
        'title' =>'园区风貌',
        'page callback' => 'about_parkstyle_page_callback',
        'access callback' => TRUE,
        'type' => MENU_LOCAL_TASK,
    );$items['about/parkstyle'] = array(
        'title' =>'农业学校',
//        'page callback' => 'about_parkstyle_page_callback',
        'access callback' => TRUE,
        'type' => MENU_LOCAL_TASK,
    );$items['about/parkstyle'] = array(
        'title' =>'园区招商',
//        'page callback' => 'about_parkstyle_page_callback',
        'access callback' => TRUE,
        'type' => MENU_LOCAL_TASK,
    );$items['about/parkstyle'] = array(
        'title' =>'入驻企业',
//        'page callback' => 'about_parkstyle_page_callback',
        'access callback' => TRUE,
        'type' => MENU_LOCAL_TASK,
    );
    return $items;
}

function about_introduction_page_callback() {
    global $user;
    $module_path = drupal_get_path('module', 'about');
//    drupal_add_css($module_path . '/css/about.css');
//   drupal_add_js();
    return theme("aboutpage_page");
}

function about_parkstyle_page_callback() {
    global $user;
    return "Hello world1";
}

/**
 * Implements hook_theme()
 */
function about_theme() {
    return array(
        'aboutpage_page' => array(
            'template' => 'aboutpage',
        ),
    );
}
